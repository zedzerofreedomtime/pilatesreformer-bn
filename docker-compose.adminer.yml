services:
  adminer:
    image: adminer:4
    container_name: pilates-adminer
    restart: unless-stopped
    ports:
      - "18082:8080"
    environment:
      ADMINER_DEFAULT_SERVER: host.docker.internal
